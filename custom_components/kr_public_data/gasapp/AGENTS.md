# `gasapp` (가스앱 - 한국가스공사)

Requires an OAuth-style token + member-id + contract number captured from the mobile app. There is no public OAuth endpoint — these values can only come from the user's own gasapp mobile session, not from a self-service key request.
